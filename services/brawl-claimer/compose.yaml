name: platform-brawl-claimer

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  brawl-claimer:
    image: ghcr.io/hsiii/brawl-stars-store-claimer:${BRAWL_CLAIMER_IMAGE_TAG:-main}
    restart: unless-stopped
    cpus: 0.5
    mem_limit: 768m
    shm_size: 512mb
    logging: *default-logging
    labels:
      dev.hsichen.platform.managed: "true"
    env_file:
      - /srv/platform/secrets/brawl-stars-claimer.env
    environment:
      BRAWL_STARS_CLAIMER_ENABLED: "false"
    healthcheck:
      test:
        - CMD
        - bun
        - -e
        - const response = await fetch('http://127.0.0.1:3100/api/health'); if (!response.ok) process.exit(1);
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 20s
    volumes:
      - brawl-claimer-state:/app/state
    networks:
      edge:
        aliases:
          - brawl-claimer

networks:
  edge:
    external: true
    name: platform_edge

volumes:
  brawl-claimer-state:
    external: true
    name: brawl-stars-claimer_state
